{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "lerna run test --stream --no-bail",
    "test:ci": "lerna run test:ci --stream --no-bail"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
